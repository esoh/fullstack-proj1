type Employee @model {
  id: ID!
  firstname: String!
  lastname: String!
  skills: [ID]
  address: [Address]
}

type Address {
  line1: String!,
  line2: String,
  city: String,
  state: String,
  zipcode: String,
}

type Skill @model {
  id: ID!
  name: String!
}
